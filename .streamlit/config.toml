[client]
showSidebarNavigation = false
toolbarMode = "auto"

[theme]
# Dark is the default. Switch via ⋮ → Settings → Theme.
# Our JS reads Streamlit's --background-color to detect the theme
# and stamps data-theme on <html>, driving our full CSS variable system.
base = "dark"
